/*--------------------------------*- C++ -*----------------------------------*\
  =========                 |
  \\      /  F ield         | OpenFOAM
   \\    /   O peration     |
    \\  /    A nd           |
     \\/     M anipulation  |
\*---------------------------------------------------------------------------*/
FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    object      controlDict;
}
// * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * //
// Shakedown A (M=3) - startup/timestep strategy still under investigation.
// adjustTimeStep/maxCo/maxDeltaT below are provisional (unchanged from the
// old production settings) - THIS IS WHAT SHAKEDOWN A WILL BE TUNING.

application     rhoCentralFoam;

startFrom       latestTime;
startTime       0;

stopAt          endTime;
endTime         0.001;

deltaT          1e-10;

writeControl    timeStep;
writeInterval   2;

purgeWrite      20;

writeFormat     ascii;
writePrecision  6;
writeCompression off;

timeFormat      general;
timePrecision   6;

runTimeModifiable true;

adjustTimeStep  yes;
maxCo           0.5;
maxDeltaT       1e-6;

functions
{
    probeStagnation
    {
        type            probes;
        libs            ("libsampling.so");
        writeControl    timeStep;
        writeInterval   1;
        fields          (T p U);
        probeLocations
        (
            (0 0 0)
        );
    }
}
// ************************************************************************* //
